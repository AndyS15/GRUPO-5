Console.WriteLine("Bienvenido, recuerde obtendra un descuento del 15% en su compra si esta supera los S/100");
     Console.WriteLine("Ingrese el precio del producto: ");
     double pdp = double.Parse(Console.ReadLine());

     Console.WriteLine();
     Console.WriteLine("Ingrese la cantidad que desea comprar: ");
     int cdp = int.Parse(Console.ReadLine());

     double subtotal = pdp * cdp;

     if (subtotal > 100)
      {
       
         double descuento = subtotal * 0.15;
         double total = subtotal - descuento;
         Console.WriteLine("El subtotal es: " + subtotal);
         Console.WriteLine("El descuento aplicado es: " + descuento);
         Console.WriteLine("El total a pagar es: " + total);
      }

   else
    {
        Console.WriteLine("El subtotal es: " + subtotal);
        Console.WriteLine("EL descuento aplicado es: 0");
        Console.WriteLine("El total a pagar es: " + subtotal);
      }
   
